apiVersion: v1
kind: Pod
metadata:
  name: "speedupdate-test-connection"
  labels:
    {{- include "speedupdate_chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: netcat
      image: securecodebox/nmap
      command: ['nmap']
      args: ['-p 20-1000 10.96.235.205']
  restartPolicy: Never
